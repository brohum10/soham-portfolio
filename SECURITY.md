# Security Policy

The portfolio is a static site and does not collect visitor data. Reports involving dependency compromise, unsafe links, unintended personal-data exposure, or script injection should be reported privately.

Use GitHub's **Security → Report a vulnerability** flow when available. Otherwise, contact the maintainer through the GitHub profile. Include the affected URL or commit, impact, and a minimal reproduction.

The deployed site and current `main` branch are supported.
